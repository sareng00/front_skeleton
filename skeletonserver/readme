npm init
npm install express morgan cors dotenv mysql2 bcrypt nunjucks chokidar

//github repository 에는 node_modules 는 올라가지 않는다..
//팀원이 clone 하면.. package.json 만 공유된다..
//npm install
//package.json 에 명시된 모듈들이 다운로드 된다.. ==> 팀장과 동일 환경이 된다..


npx nodemon app.js
http://localhost:8000